% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ivreg2-class.R
\name{model.matrix.ivreg2}
\alias{model.matrix.ivreg2}
\title{Extract design matrices from an ivreg2 model}
\usage{
\method{model.matrix}{ivreg2}(
  object,
  component = c("regressors", "projected", "instruments"),
  ...
)
}
\arguments{
\item{object}{An object of class \code{"ivreg2"}.}

\item{component}{Character: which matrix to return. \code{"regressors"}
(default) returns the regressor matrix X; \code{"instruments"} returns
the full instrument matrix Z (\code{NULL} for OLS); \code{"projected"} returns
the projected regressors X_hat (equals X for OLS).}

\item{...}{Additional arguments (ignored).}
}
\value{
A numeric matrix, or \code{NULL} if \code{component = "instruments"}
for an OLS model.
}
\description{
Returns the regressor matrix (X), instrument matrix (Z), or projected
regressors (X_hat = P_Z X). Matrices are retrieved from the stored
\code{x} component if available (when \code{ivreg2(..., x = TRUE)} was used),
otherwise reconstructed from the model frame.
}
\details{
For models estimated with \code{partial}, the stored matrices (when
\code{x = TRUE}) are the post-partialling matrices. Reconstruction from the
model frame returns pre-partialling matrices.
}

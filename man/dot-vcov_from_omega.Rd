% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov-robust.R
\name{.vcov_from_omega}
\alias{.vcov_from_omega}
\title{Assemble a sandwich VCV from the L x L moment covariance Omega}
\usage{
.vcov_from_omega(
  bread,
  A,
  omega,
  N,
  K,
  M = NULL,
  small = FALSE,
  dofminus = 0L,
  sdofminus = 0L,
  cluster = FALSE
)
}
\arguments{
\item{bread}{K x K bread matrix (k-class bread for LIML/k-class).}

\item{A}{L x K first-stage coefficient matrix, or \code{NULL} for OLS
(where Z = X and the congruence is the identity).}

\item{omega}{L x L moment covariance from \code{\link[=.compute_moment_cov]{.compute_moment_cov()}}
(psd-corrected there when \code{psd} is set).}

\item{N}{Integer: number of observations.}

\item{K}{Integer: number of regressors.}

\item{M}{Integer: number of clusters (required when \code{cluster = TRUE}).}

\item{small}{Logical: apply the finite-sample correction.}

\item{dofminus, sdofminus}{Integer DoF adjustments.}

\item{cluster}{Logical: \code{TRUE} for the cluster-family small-sample factor
\code{(N-1)/(N-K-sdofminus) * M/(M-1)}; \code{FALSE} for the robust-family factor
\code{(N-dofminus)/(N-K-dofminus-sdofminus)}.}
}
\value{
K x K variance-covariance matrix.
}
\description{
Stata assembles every plain (non-GMM) robust-family VCV from the L x L
instrument-space moment covariance returned by \code{m_omega}, by congruence
with the first-stage map A (\verb{X_hat = Z A}): \verb{V = 1/N * aux5 * omega * aux5'}
(s_iegmm, ivreg2.ado:5556-5558). In unscaled R quantities that is
\verb{V = N * bread * (A' Omega A) * bread}, followed by the small-sample
factor. This route is used when \code{psd} is set, so the psd correction is
applied to Omega \emph{before} the congruence — \verb{psd(A' S A) != A' psd(S) A},
which is why correcting the K x K meat (or the final VCV) diverges from
Stata whenever the correction binds.
}
\details{
The single formula reproduces every plain path exactly because each
VCE type's Omega normalization cancels its sandwich scaling:
HC and HAC use \code{Omega = meat/(N - dofminus)} with `V = bread meat bread
\itemize{
\item N/(N - dofminus)\verb{; cluster, two-way, DK, and cluster+kernel use }Omega = meat/N\code{with}V = bread meat bread\verb{; AC and Stock-Watson Omegas are already per-observation with }V = bread Omega bread * N`.
}
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-gmm.R
\name{.fit_gmm_wmatrix}
\alias{.fit_gmm_wmatrix}
\title{Fit GMM with user-supplied weighting matrix (inefficient GMM)}
\usage{
.fit_gmm_wmatrix(
  parsed,
  small = FALSE,
  dofminus = 0L,
  sdofminus = 0L,
  W,
  omega_fn,
  omega_rank_bound = Inf
)
}
\arguments{
\item{parsed}{A \code{parsed_formula} object from \code{.parse_formula()}.}

\item{small}{Logical: if \code{TRUE}, use \code{N-K} denominator for sigma.}

\item{dofminus}{Integer: large-sample DoF adjustment (default 0).}

\item{sdofminus}{Integer: small-sample DoF adjustment (default 0).}

\item{W}{L x L user-supplied weighting matrix.}

\item{omega_fn}{Function: closure that takes a residual vector and returns
the L x L moment covariance matrix Omega.}
}
\value{
A named list with the same fields as \code{.fit_gmm2s()} plus \code{method = "gmmw"}.
}
\description{
Computes GMM estimates using user W as the weighting matrix. The VCV is
the full sandwich form (not the collapsed efficient form), and the J
statistic is computed via efficient 2-step re-estimation using the
estimated Omega (not the wmatrix-weighted residuals).
}
\details{
Mirrors Stata's \code{s_iegmm()} (ivreg2.ado:5495-5563).
}
\keyword{internal}

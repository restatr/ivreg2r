% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-accessor.R
\name{diagnostics}
\alias{diagnostics}
\alias{diagnostics.ivreg2}
\title{Extract IV diagnostic tests as a tibble}
\usage{
diagnostics(x, ...)

\method{diagnostics}{ivreg2}(x, ...)
}
\arguments{
\item{x}{A fitted model object.}

\item{...}{Additional arguments (ignored).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per diagnostic test present on
the model and the following columns:
\describe{
\item{\code{test}}{Character. A stable machine-readable key, meant for
\code{filter(test == ...)}-style selection (e.g. \code{"overid"},
\code{"weak_id_robust"}, \code{"endogeneity"}).}
\item{\code{test_name}}{Character. The display name printed by
\code{summary()}.}
\item{\code{statistic}}{Double. The test statistic, or — for the
Stock-Yogo rows — the critical value rather than a statistic.}
\item{\code{df}}{Integer. Degrees of freedom, \code{NA} when not applicable.}
\item{\code{df2}}{Integer. Denominator degrees of freedom, \code{NA} unless the
statistic is F-distributed.}
\item{\code{p_value}}{Double. \code{NA} when not defined (e.g. Stock-Yogo
critical values, or an exactly identified overidentification row).}
\item{\code{tested_vars}}{Character. The variable(s) tested; populated only
for the endogeneity, orthogonality, and redundancy rows.}
}

Rows appear only for the tests actually computed on the model: an IV
fit reports underidentification, weak identification,
overidentification, and — for every endogenous regressor, unless
narrowed to a subset via \verb{endog =} — the endogeneity test by default,
while \verb{orthog =} and \verb{redundant =} add the corresponding orthogonality
and redundancy rows only when the user requests them.
The Stock-Yogo rows (keys of the form \code{"sy_iv_size_10"}) report critical
values, not test statistics, so their \code{p_value} is always \code{NA}. An OLS
fit (single-part formula) has no diagnostics at all and returns a
zero-row tibble with the columns above.
}
\description{
Flattens the IV specification tests stored on a fitted model into a single
tibble, one row per test, so they can be filtered, joined, or printed like
any other tidyverse output instead of navigated as a nested list
(\code{x$diagnostics$overid$stat} and friends).
}
\examples{
data(card)
fit <- ivreg2(
  lwage ~ exper + expersq + black + south | educ | nearc4 + nearc2,
  data = card, vcov = "robust"
)
diagnostics(fit)

}
\seealso{
\code{\link[=first_stage]{first_stage()}}, \code{\link[=glance.ivreg2]{glance.ivreg2()}}

Other ivreg2 methods:
\code{\link[=coef.ivreg2]{coef.ivreg2()}},
\code{\link[=confint.ivreg2]{confint.ivreg2()}},
\code{\link[=first_stage]{first_stage()}},
\code{\link[=fitted.ivreg2]{fitted.ivreg2()}},
\code{\link[=formula.ivreg2]{formula.ivreg2()}},
\code{\link[=ivreg2]{ivreg2()}},
\code{\link[=model.matrix.ivreg2]{model.matrix.ivreg2()}},
\code{\link[=nobs.ivreg2]{nobs.ivreg2()}},
\code{\link[=predict.ivreg2]{predict.ivreg2()}},
\code{\link[=print.ivreg2]{print.ivreg2()}},
\code{\link[=print.summary.ivreg2]{print.summary.ivreg2()}},
\code{\link[=residuals.ivreg2]{residuals.ivreg2()}},
\code{\link[=summary.ivreg2]{summary.ivreg2()}},
\code{\link[=terms.ivreg2]{terms.ivreg2()}},
\code{\link[=update.ivreg2]{update.ivreg2()}},
\code{\link[=vcov.ivreg2]{vcov.ivreg2()}}
}
\concept{ivreg2 methods}

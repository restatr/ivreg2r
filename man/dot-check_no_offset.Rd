% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse-formula.R
\name{.check_no_offset}
\alias{.check_no_offset}
\title{Reject offset() terms in any formula part}
\usage{
.check_no_offset(formula, n_rhs)
}
\arguments{
\item{formula}{A \code{Formula} object (already validated by
\code{.check_formula_parts()}).}

\item{n_rhs}{Integer: number of RHS parts (1 or 3).}
}
\value{
Invisible \code{NULL}. Stops if any part contains an \code{offset()} term.
}
\description{
Stata's \code{ivreg2} has no offset concept, so there is no parity target to
implement against; a silently-ignored offset (the model frame never calls
\code{model.offset()}) is worse than an explicit rejection. \code{terms()} sets a
non-\code{NULL} \code{"offset"} attribute only for the \code{offset(...)} function form,
so a column merely named \code{offset} used as an ordinary variable is
unaffected.
}
\keyword{internal}

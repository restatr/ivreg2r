% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-overid.R
\name{.compute_ar_liml_overid}
\alias{.compute_ar_liml_overid}
\title{Anderson-Rubin LIML overidentification statistics}
\usage{
.compute_ar_liml_overid(lambda, N, overid_df, dofminus = 0L)
}
\arguments{
\item{lambda}{Numeric: LIML eigenvalue from \code{.fit_kclass()}.}

\item{N}{Integer: number of observations.}

\item{overid_df}{Integer: degree of overidentification (L - K).}

\item{dofminus}{Integer: large-sample DoF adjustment (default 0).}
}
\value{
Named list with \code{lr_stat}, \code{lr_p}, \code{lin_stat}, \code{lin_p}, \code{df},
or a zero-stat placeholder when exactly identified (df == 0).
}
\description{
Computes the LR and linearized forms of the Anderson-Rubin
overidentification test for LIML estimation under IID errors.
}
\details{
Both statistics are chi-squared(L - K) under the null that all
overidentifying restrictions are valid. The LR form uses
\eqn{(N - \text{dofminus}) \ln(\lambda)}{(N - dofminus) * ln(lambda)}
and the linearized form uses
\eqn{(N - \text{dofminus})(\lambda - 1)}{(N - dofminus) * (lambda - 1)}.
}
\keyword{internal}

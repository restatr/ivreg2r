% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-id.R
\name{.clamp_psd_eigenvalues}
\alias{.clamp_psd_eigenvalues}
\title{Clamp negative eigenvalues of a symmetric matrix to zero}
\usage{
.clamp_psd_eigenvalues(d)
}
\arguments{
\item{d}{Numeric vector of eigenvalues (from \code{eigen(..., symmetric = TRUE)}).}
}
\value{
List with \code{values} (eigenvalues with negatives clamped to zero),
\code{non_psd} (logical: a non-noise negative eigenvalue was present), and
\code{most_negative} (the smallest eigenvalue before clamping, or 0 if none
were negative) for use in a caller's warning message.
}
\description{
Shared helper for the eigendecomposition-based routines that project a
matrix onto the nearest positive-semidefinite one. Negative eigenvalues
that are only floating-point round-off are clamped to zero silently; the
\code{non_psd} flag is set only when a negative eigenvalue is too large to be
noise, so the caller can decide whether to warn.
}
\details{
The tolerance is relative to the largest eigenvalue magnitude but carries an
absolute floor at machine epsilon, so a matrix that is numerically zero
throughout (all eigenvalues near the round-off floor) is treated as noise
rather than as a genuinely non-PSD input.
}
\keyword{internal}

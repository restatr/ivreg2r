# Check for duplicate variables across formula parts

Check for duplicate variables across formula parts

## Usage

``` r
.check_duplicates(formula)
```

## Arguments

- formula:

  A 3-part `Formula` object.

## Value

Invisible `NULL`. Stops with an error naming all duplicates.
